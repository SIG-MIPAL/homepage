# Management / editing backend - local only, never pushed (backed up via OneDrive instead).
admin/
join/

# Claude project notes - local only (describes the local admin backend, not for the public repo).
CLAUDE.md

# Distributable member onboarding template - shared directly, not via this public repo.
people/person-template.zip

# Node
node_modules/
npm-debug.log*
